[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "gpt_nexus"
version = "0.1.0"
description = "GPT Agent Orchestration Core"
authors = [{ name = "Patrick" }]
dependencies = []

[tool.setuptools]
packages = ["gpt_nexus"]
